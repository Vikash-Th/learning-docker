# Learning Docker 

This repository contains my setups for learning Docker. It includes two automated tasks: a system info reporter and a multi-container data network.

---
## Task 1: Robot Info Container
This task builds an isolated environment that automatically checks and reports system data. You can build the image and run it automatically using this single command:
## Task 2: Sensor-Logger Network
This task connects two separate containers together. A virtual sensor container creates continuous data files inside a shared folder, while a logger container reads and displays those files in real time. 
